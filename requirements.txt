# List of dependencies for the AI Coding Agent

# For Google's Gemini API
google-generativeai

# For OpenAI's API
openai

# For creating embeddings locally and interacting with the vector store
chromadb
sentence-transformers

# For splitting text into chunks during indexing
langchain

# For rich terminal output (colors, markdown)
rich

# For progress bars during indexing
tqdm

# For parsing command-line arguments (standard library, but good to note)
argparse
